// Stub — Y7/Y10 subjects are now fully dynamic (DB-loaded curricula).
// getY7ShortLabel is kept as a pass-through so callers don't need to change.
export function getY7ShortLabel(n) { return n || '' }
export function getY7TopicConfig() { return null }
